#version 400 core

// Input vertex data, different for all executions of this shader.
uniform float time;
out float someFloat;
in vec3 position;

void main(){
    gl_Position = vec4(position, 1.0);
    someFloat = position.x + 0.5;
}